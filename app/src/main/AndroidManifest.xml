<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.learningcards">

    <application
        android:allowBackup="true"
        android:icon="@drawable/fucking_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".activities.ThemeChoiceActivity"></activity>
        <activity android:name=".activities.StatActivity" />
        <activity android:name=".activities.ChangeWordActivity" />
        <activity android:name=".activities.DictionaryActivity" />
        <activity android:name=".activities.AddWordActivity" />
        <activity android:name=".activities.CardsActivity" />
        <activity android:name=".activities.CardsChoiceActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>