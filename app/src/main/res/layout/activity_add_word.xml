<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.LinearLayoutCompat xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimary"
    android:orientation="vertical"
    tools:context=".activities.AddWordActivity">

    <EditText
        android:id="@+id/russian"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Русский перевод"
        android:importantForAutofill="no" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <Button
            android:id="@+id/addChineseButton"
            android:layout_width="205dp"
            android:layout_height="wrap_content"
            android:onClick="addChinese"
            android:text="Добавить китайский" />

        <Button
            android:id="@+id/addEnglishButton"
            android:layout_width="205dp"
            android:layout_height="wrap_content"
            android:onClick="addEnglish"
            android:text="Добавить англ." />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/foreignWord"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <Button
            android:id="@+id/addGroupButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="80"

            android:onClick="addGroup"
            android:text="Указать группу слова"></Button>
    </LinearLayout>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="addWordToDatabase"
        android:text="Закончить и добавить слово" />


</androidx.appcompat.widget.LinearLayoutCompat>